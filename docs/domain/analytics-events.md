# Analytics Events

## Naming
Use `domain_action_subject` naming (example: `commerce_start_purchase`).

## Event contract
Each event must document:
- Trigger point
- Required payload fields
- Privacy classification
- Downstream dashboard owner
